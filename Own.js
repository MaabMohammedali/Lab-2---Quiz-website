function addQuestion() {
    // Get values from 
    var questionText = document.getElementById('question-text').value;
     // Create an object 
    var question = {
        text: questionText,
    };
}